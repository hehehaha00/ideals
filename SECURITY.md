# 安全说明

请不要在公开 Issue 中粘贴模型 key、`.env.local`、完整服务器日志或用户内容。

本项目的模型 key 只应保存在运行服务端的环境变量或部署平台 secret 中，不能放进前端代码、浏览器存储或公开配置文件。若发现密钥泄露，请立即在对应厂商后台撤销并重新生成。

当前 Node 代理默认面向本机开发环境。部署到公网前，需要通过反向代理限制来源、启用 HTTPS，并增加服务端访问控制和请求限流。
