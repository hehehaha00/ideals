# 参与贡献

感谢你改进脑洞实验室。提交改动前，请先确认：

1. 不提交 `.env.local`、真实模型 key、用户数据或本地日志。
2. 在 `app/` 中运行 `npm test` 和 `npm run build`。
3. 功能改动需附对应测试，并保持现有本地工作区数据兼容。
4. 模型接入需明确协议边界；OpenAI 兼容接口使用现有提供商预设，其他协议通过独立 adapter 实现。

问题和合并请求请说明目标、主要改动、验证结果，以及是否影响已有本地数据。
